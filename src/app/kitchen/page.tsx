"use client";
import Dashboard from '@/components/Kitchen/Dashboard';

/**
 * The entry point for the kitchen staff interface.
 * Renders the Dashboard component.
 *
 * @component
 * @returns {JSX.Element} The rendered kitchen page.
 */
export default function KitchenPage() {
    return <Dashboard />;
}
